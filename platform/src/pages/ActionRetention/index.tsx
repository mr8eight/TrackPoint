const ActionRetention: React.FC = () => {
  const arr = new Array(1).fill(0);
  return (
    <>
      {arr.map(() => (
        <div>ActionRetention</div>
      ))}
    </>
  );
};

export default ActionRetention;
